# Copy this file to .env and fill in your real Hetzner Cloud API token.
# Never commit the real .env file (the repo's root .gitignore already
# excludes any file named .env at any depth).
HCLOUD_TOKEN=changeme
